(function ($) {
    $(function () {
        // todo: we need this to be initialised when content is loaded into a modal
        initForm(true);
        initRelationFields();
        initEditRelations();
    });


})(jQuery);