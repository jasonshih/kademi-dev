function initPromotionsApp(orgRoot, webRoot, enabled, repoDir) {
    var rewardsApp = applications.rewards;
    log.info("initPromotionsApp: orgRoot={} app={}", orgRoot, rewardsApp);
    var alertsApp = applications.alerts;
    
    // TODO: default setup
}